// Agent IDs produced by server/src/simulator.js - kept separate so they can
// be hidden from the dashboard when looking at real agent activity.
export const DEMO_AGENT_IDS = new Set(['agent-builder', 'agent-researcher', 'agent-reviewer']);
